<p>From <strong>@{{ user_name }}</strong> in <a href="{{ url }}">Slack#{{ channel_name }}</a> on {{ timestamp }}:</p>
<blockquote style="color: rgb(96, 96, 96);">
{% for line in text %}<p>{{ line | trim | escape }}</p>{% endfor %}
</blockquote>
